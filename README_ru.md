# Marzban Shop

> 🇬🇧 English version: [README.md](README.md)

Telegram-бот для продажи и продления VPN-подписок через Marzban. Бот работает на aiogram v3, поддерживает несколько способов оплаты, локализацию и вебхуки.

## Возможности
- Ручные оплаты по коротким ссылкам (Сбер / Т-Банк)
- Платежи YooKassa, Cryptomus и Telegram Stars
- Триал с управлением через переменные окружения
- Интерфейс на русском и английском
- CLI для управления ботом и nginx
- Скрипт быстрой установки

## Требования
- Сервер Linux с root-доступом
- Docker и Docker Compose plugin
- nginx с валидными TLS-сертификатами (Let’s Encrypt рекомендуется)
- Токен Telegram-бота и доступ к панели Marzban

## Быстрая установка (one-click)
1. Скачайте и запустите установщик (при необходимости обновите REPO_URL в `install.sh` на адрес вашего форка):
   ```bash
   curl -O https://raw.githubusercontent.com/indie-master/marzban-shop/main/install.sh
   chmod +x install.sh
   sudo ./install.sh
   ```
2. Скрипт может настроить nginx и получить сертификат, если указать домен, либо пропустить этот шаг и оставить конфигурацию на потом (используйте примеры из `nginx/*.example` и CLI `mnginx`).
3. Заполните ключевые значения `.env` (копия `.env.example`) и при необходимости шаблон тарифов.
4. Управляйте ботом через `mshop`, nginx — через `mnginx`.

## Настройка

### Файл окружения (`.env`)
В репозитории лежит `.env.example` с плейсхолдерами; при установке его можно скопировать в `.env` (скрипт предложит это сделать). Реальный `.env` в git не хранится. Основные переменные (значения-заглушки, замените на свои):
- `BOT_TOKEN` – токен Telegram-бота
- `SHOP_NAME` – отображаемое имя магазина
- `TEST_PERIOD` – включить/выключить кнопку триала (`true`/`false`)
- `TEST_PERIOD_DAYS` – длительность триала в днях
- `PANEL_HOST`, `PANEL_GLOBAL`, `PANEL_USER`, `PANEL_PASS` – доступ к панели Marzban (адрес API и внешний адрес подписок)
- `PROTOCOLS_CONFIG` – путь к конфигу протоколов (например, `protocols.json`)
- Платежи: `YOOKASSA_TOKEN`, `YOOKASSA_SHOPID`, `CRYPTO_TOKEN`, `MERCHANT_UUID`
- Ручные оплаты: `PAY_SBER_URL`, `PAY_TBANK_URL`
- Telegram Stars: `STARS_PAYMENT_ENABLED=true` (любое другое значение выключает Stars)
- Админка и уведомления: `TG_INFO_CHANEL`, `ADMIN_IDS` (через запятую), `TG_BACKUP_BOT_TOKEN`, `TG_BACKUP_CHAT_ID`
- База данных: `DB_NAME`, `DB_USER`, `DB_PASS`, `DB_ROOT_PASS`, `DB_ADDRESS`, `DB_PORT`
- Вебхук: `WEBHOOK_URL` (без `/webhook`, например `https://bot.example.com`), `WEBHOOK_PORT`
- Уведомления: `RENEW_NOTIFICATION_TIME`, `EXPIRED_NOTIFICATION_TIME`
- FAQ: `FAQ_PRIVACY_URL`/`FAQ_PRIVACY_ENABLED`, `FAQ_TERMS_URL`/`FAQ_TERMS_ENABLED`, `FAQ_RULES_URL`/`FAQ_RULES_ENABLED`, `FAQ_OFFER_URL`/`FAQ_OFFER_ENABLED`
- Ссылки на Happ: `HAPP_IOS_URL`, `HAPP_ANDROID_PLAY_URL`, `HAPP_ANDROID_APK_URL`, `HAPP_WINDOWS_URL`, `HAPP_LINUX_URL`
- Картинки разделов: `START_IMAGE_ENABLED`/`START_IMAGE_PATH`, `SUBSCRIPTION_IMAGE_ENABLED`/`SUBSCRIPTION_IMAGE_PATH`, `INSTRUCTIONS_IMAGE_ENABLED`/`INSTRUCTIONS_IMAGE_PATH`, `FAQ_IMAGE_ENABLED`/`FAQ_IMAGE_PATH`, `SUPPORT_IMAGE_ENABLED`/`SUPPORT_IMAGE_PATH`

Редактируйте файл вручную или через `mshop edit .env` после установки. Настоящий `.env` не коммитьте в репозиторий.

### Настройка вебхука
1. Укажите `WEBHOOK_URL` в `.env` **без** суффикса `/webhook` (пример: `https://bot.example.com`).
2. Настройте nginx, чтобы `https://домен/webhook` проксировался на бота (см. `nginx/bot.conf.example`).
3. Перезапустите бота: `mshop restart`.
4. Управляйте вебхуком через CLI:
   - `mshop webhook-info` — посмотреть текущий вебхук
   - `mshop webhook-set` — установить вебхук на `${WEBHOOK_URL}/webhook`
   - `mshop webhook-delete` — удалить вебхук
   - `mshop webhook-reset` — удалить и сразу установить вебхук
5. Быстрая проверка:
   ```bash
   curl -I https://your-domain/webhook
   ```

### Экраны и навигация бота
- **Приветствие (`/start`)** показывает приветственный текст с лозунгом, списком локаций и строкой про бесплатный период, если он доступен (`TEST_PERIOD`/`TEST_PERIOD_DAYS`).
- **Моя подписка** выводит статус (активна/не активна), оставшиеся дни, дату окончания (`дд.мм.гггг`), остаток трафика (или ♾️) и копируемую ссылку подписки; WebApp-кнопка сохраняется.
- **Инструкции** содержат выбор ОС: 🍏 iOS / MacOS, 🤖 Android, 🖥️ Windows, 👨🏻‍💻 Linux. Для каждой платформы показаны шаги и кнопки скачивания из переменных `HAPP_*`, плюс кнопка возврата к списку ОС.
- **Частые вопросы** — набор ссылок (политика, соглашение, правила, оферта) с управлением через пары `FAQ_*_ENABLED` и `FAQ_*_URL`.
- **Поддержка** — кнопка «Написать в поддержку» по `SUPPORT_LINK` и возврат назад.
- Опциональные картинки перед разделами настраиваются парами `*_IMAGE_ENABLED` и `*_IMAGE_PATH` для приветствия, подписки, инструкций, FAQ и поддержки (путь может быть локальным файлом или ссылкой HTTP/HTTPS).

### Настройка тарифов (`goods.json`)
Файл `goods.example.json` содержит минимальный пример; скопируйте его в `goods.json` при установке или позже. Реальный `goods.json` не хранится в git. Пример структуры:
```json
[
  {
    "title": "Basic VPN",
    "price": {"en": 1, "ru": 100, "stars": 50},
    "callback": "basic_vpn",
    "months": 1,
    "description": "Example tariff; replace with your real plans"
  }
]
```
Обновите названия, цены, срок и callback под свои тарифы.

### Конфиг протоколов
`protocols.json` описывает протоколы и имена inbound в Marzban. Приведите его в соответствие с панелью и укажите `PROTOCOLS_CONFIG=protocols.json` в `.env`.

## Настройка nginx
Примеры лежат в каталоге `nginx/`:
- `nginx/nginx.conf.example` — базовый конфиг nginx с необязательным блоком `stream {}` для TLS passthrough/SNI и совместного использования 443 порта.
- `nginx/bot.conf.example` — конфиг сайта с редиректом HTTP→HTTPS, поддержкой Let’s Encrypt, прокси для вебхука (`/webhook` → `127.0.0.1:8080/webhook`) и плейсхолдерами ссылок оплаты (`/pay/sber`, `/pay/tbank`).

Шаги (их автоматизирует `install.sh`):
1. Скопируйте примеры в `/etc/nginx/nginx.conf` и `/etc/nginx/sites-available/bot.conf`, поменяв домен.
2. Создайте симлинк `/etc/nginx/sites-enabled/bot.conf`.
3. Проверьте конфиг `nginx -t` и перезагрузите nginx.
4. Выпустите сертификаты: `certbot --nginx -d bot.example.com --email admin@example.com --agree-tos --redirect`.

## CLI для бота (`mshop`)
Устанавливается в `/usr/local/bin/mshop`.

Команды:
- `mshop status` — статус контейнеров
- `mshop start` / `mshop stop` / `mshop restart`
- `mshop reload` — перезапуск контейнера бота
- `mshop logs` — логи бота
- `mshop edit .env|docker-compose.yml|goods.json|protocols.json|db` — редактирование конфигов или запуск клиента БД (с предупреждением)
- `mshop update` — обновить репозиторий и образы, перезапустить
- `mshop backup-db` — бэкап БД (отправка в Telegram при наличии `TG_BACKUP_BOT_TOKEN` и `TG_BACKUP_CHAT_ID`)
- `mshop restore-db <backup.tar.gz>` — восстановление БД
- `mshop webhook-info|webhook-set|webhook-delete|webhook-reset` — управление вебхуком Telegram через `WEBHOOK_URL` + `/webhook`
- `mshop help` — справка

## CLI для nginx (`mnginx`)
Устанавливается в `/usr/local/bin/mnginx`.

Команды:
- `mnginx status|start|stop|restart`
- `mnginx enable` — включить сервис и симлинк сайта
- `mnginx reload` / `mnginx test`
- `mnginx logs` — логи nginx
- `mnginx update` — обновить конфиги из репозитория и перезагрузить
- `mnginx version` — версия nginx
- `mnginx help` — справка

## Бэкап и восстановление БД
- Бэкап: `mshop backup-db` (архив в `backups/`, при необходимости отправляется в Telegram).
- Восстановление: `mshop restore-db <путь к архиву>` (перед выполнением запрашивает подтверждение).

## Обновление
- Бот и сервисы: `mshop update`
- Конфиги nginx: `mnginx update`

## Диагностика
- Логи бота: `mshop logs`
- Проверка конфига nginx: `mnginx test`
- Перезагрузка nginx: `mnginx reload`
- Проверка вебхука: `curl -I https://ваш-домен/webhook`

Убедитесь, что домен указывает на сервер и сертификаты действительны при использовании вебхука.
